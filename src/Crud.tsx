import {useEffect,useState}from'react';import{archive,listMotives,listOffers,saveMotive,saveOffer}from'./phase2Api';import type{Motive,Offer}from'./types';
type Kind='offers'|'motives';
export default function Crud({kind}:{kind:Kind}){const[rows,setRows]=useState<(Offer|Motive)[]>([]),[editing,setEditing]=useState<any>(null),[error,setError]=useState('');const load=()=>((kind==='offers'?listOffers():listMotives())as Promise<any[]>).then(setRows).catch(e=>setError(e.message));useEffect(()=>{load()},[kind]);const empty=kind==='offers'?{name:'',short_name:'',description:'',offer_type:'tour',location:'',status:'draft',target_segments:[]}:{name:'',description:'',hypothesis:'',status:'draft',target_segments:[]};async function save(){try{kind==='offers'?await saveOffer(editing):await saveMotive(editing);setEditing(null);load()}catch(e){setError((e as Error).message)}}async function doArchive(id:string){try{await archive(kind==='offers'?'lms_offers':'lms_motives',id);load()}catch(e){setError((e as Error).message)}}return <><div className="page-head"><div><h1>{kind==='offers'?'Offers':'Motives'}</h1><p>{kind==='offers'?'The experiences we want people to choose.':'The underlying ideas we want to test. Motives are not copy.'}</p></div><button className="primary" onClick={()=>setEditing({...empty})}>New {kind==='offers'?'offer':'motive'}</button></div>{error&&<div className="error">{error}</div>}<div className="table-wrap"><table><thead><tr><th>Name</th><th>{kind==='offers'?'Location':'Hypothesis'}</th><th>Status</th><th/></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td><b>{r.name}</b><div className="muted">{r.description}</div></td><td>{kind==='offers'?r.location:r.hypothesis}</td><td>{r.status}</td><td><button onClick={()=>setEditing({...r})}>Edit</button> <button onClick={()=>doArchive(r.id)}>Archive</button></td></tr>)}</tbody></table></div>{editing&&<div className="overlay" onClick={()=>setEditing(null)}><div className="modal" onClick={e=>e.stopPropagation()}><h2>{editing.id?'Edit':'New'} {kind==='offers'?'offer':'motive'}</h2><label>Name<input value={editing.name??''} onChange={e=>setEditing({...editing,name:e.target.value})}/></label>{kind==='offers'&&<><label>Short name<input value={editing.short_name??''} onChange={e=>setEditing({...editing,short_name:e.target.value})}/></label><label>Location<input value={editing.location??''} onChange={e=>setEditing({...editing,location:e.target.value})}/></label></>}<label>Description<textarea value={editing.description??''} onChange={e=>setEditing({...editing,description:e.target.value})}/></label>{kind==='motives'&&<label>Hypothesis<textarea value={editing.hypothesis??''} onChange={e=>setEditing({...editing,hypothesis:e.target.value})}/></label>}<label>Status<select value={editing.status??'draft'} onChange={e=>setEditing({...editing,status:e.target.value})}><option>draft</option><option>active</option><option>archived</option></select></label><div className="actions"><button onClick={()=>setEditing(null)}>Cancel</button><button className="primary" onClick={save}>Save</button></div></div></div>}</>}
