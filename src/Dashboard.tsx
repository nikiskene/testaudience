import { useEffect,useState } from 'react'; import { getStats } from './api';
export default function Dashboard(){const [s,setS]=useState<Record<string,number>|null>(null);const [e,setE]=useState('');useEffect(()=>{getStats().then(setS).catch(x=>setE(x.message))},[]);if(e)return <div className="error">{e}</div>;if(!s)return <p>Loading…</p>;const cards=[['Synthetic Minds',s.total_minds.toLocaleString()],['Countries',String(s.countries)],['Industries',String(s.industries)],['Average Curiosity',Math.round(s.avg_curiosity).toString()],['Average Skepticism',Math.round(s.avg_skepticism).toString()],['High Purchasing Power',Math.round(s.high_power_share)+'%']];return <><div className="page-head"><div><h1>Dashboard</h1><p>Overview of the Synthetic Market</p></div></div><div className="cards">{cards.map(([a,b])=><div className="card" key={a}><span>{a}</span><strong>{b}</strong></div>)}</div><h3>Recent Simulations</h3><div className="empty">No simulations yet.</div></>}
