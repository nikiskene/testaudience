import{createClient}from'npm:@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
const keys=['curiosity_gap','specificity','credibility','novelty','intellectual_tension','personal_relevance','usefulness','urgency','emotional_resonance','sales_smell','risk_reduction','social_proof','identity_relevance','fear_pressure','opportunity_pull','proposition_clarity','cta_strength','narrative_pull','subject_open_pull'] as const;
const properties:Object=Object.fromEntries(keys.map(k=>[k,{type:'integer',minimum:0,maximum:100}]));
const schema={type:'object',additionalProperties:false,required:[...keys,'summary','key_themes'],properties:{...properties,summary:{type:'string'},key_themes:{type:'array',items:{type:'string'},maxItems:8}}};
function outputText(data:any){if(typeof data.output_text==='string')return data.output_text;for(const item of data.output??[])for(const c of item.content??[])if(typeof c.text==='string')return c.text;return''}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const auth=req.headers.get('Authorization');if(!auth)throw new Error('Missing authorization');const url=Deno.env.get('SUPABASE_URL')!;const anon=Deno.env.get('SUPABASE_ANON_KEY')!;const supabase=createClient(url,anon,{global:{headers:{Authorization:auth}}});const{message_id}=await req.json();if(!message_id)throw new Error('message_id required');const{data:m,error:me}=await supabase.from('lms_messages').select('*').eq('id',message_id).single();if(me)throw me;const[{data:o},{data:mot}]=await Promise.all([supabase.from('lms_offers').select('name,description,offer_type,location,start_date,end_date,price,currency').eq('id',m.offer_id).maybeSingle(),m.motive_id?supabase.from('lms_motives').select('name,description,hypothesis').eq('id',m.motive_id).maybeSingle():Promise.resolve({data:null})]);const apiKey=Deno.env.get('OPENAI_API_KEY');const model=Deno.env.get('OPENAI_MODEL');if(!apiKey||!model)throw new Error('Set OPENAI_API_KEY and OPENAI_MODEL as Supabase Edge Function secrets');const prompt=`You are the semantic interpreter for a synthetic-market simulator. Analyze meaning, not marketing quality in the abstract. Read the subject and body as a busy skeptical senior professional would encounter them. Score each dimension 0-100. A score is descriptive, not flattering. sales_smell means how strongly it feels like familiar promotional copy. subject_open_pull means intrinsic probability pressure created by the subject itself before reading the body. personal_relevance means how clearly the text connects to a reader's current work/life problem without knowing a specific persona. intellectual_tension measures paradox, contradiction or unresolved thought. risk_reduction measures how much the proposition reduces perceived downside. Return a concise summary and up to 8 semantic themes.\n\nOFFER:\n${JSON.stringify(o)}\n\nMOTIVE:\n${JSON.stringify(mot)}\n\nSUBJECT:\n${m.subject_line}\n\nBODY:\n${m.body}`;const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Authorization':`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model,input:prompt,text:{format:{type:'json_schema',name:'semantic_profile',strict:true,schema}}})});const raw=await r.json();if(!r.ok)throw new Error(raw?.error?.message??`LLM error ${r.status}`);const text=outputText(raw);if(!text)throw new Error('Semantic model returned no text');const parsed=JSON.parse(text);const payload={message_id,...parsed,analysis_source:'llm',model_name:model,updated_at:new Date().toISOString()};const{data:profile,error:pe}=await supabase.from('lms_message_semantics').upsert(payload,{onConflict:'message_id'}).select('*').single();if(pe)throw pe;return new Response(JSON.stringify({profile}),{headers:{...cors,'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:String(e)}),{status:400,headers:{...cors,'Content-Type':'application/json'}})}});
